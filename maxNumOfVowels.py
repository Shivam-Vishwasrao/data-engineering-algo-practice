s = "abciiidef"
k = 3

def maxVowlesInWindow(s,k):
    vowelCount = 0
    maxVowelsInWindow = 0
    vowels = {'a', 'e', 'i', 'o', 'u'}
    print("hi")

    # current_window = s[0:k]
    # for i in range(len(current_window)):
    #     if i[s] in vowels:
    #         vowelCount += 1
    #     maxVowelsInWindow = max(vowelCount, maxVowelsInWindow)
    #     vowelCount = 0
    # for i in range(k, len(s)):

maxVowlesInWindow(s,k)
